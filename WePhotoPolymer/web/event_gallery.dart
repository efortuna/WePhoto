import 'package:polymer/polymer.dart';

/**
 * A Polymer event-gallery element.
 */
@CustomTag('event-gallery')
class EventGallery extends PolymerElement {

  /// Constructor used to create instance of EventGallery.
  EventGallery.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of event-gallery is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of event-gallery is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// event-gallery is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when event-gallery has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
